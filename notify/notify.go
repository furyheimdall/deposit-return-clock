// Package notify is the seat for deadline reminders (D-7 / D-3 / due).
// Domain logic is out of scope for the #2 scaffold (see issue #5).
package notify

// Seat identifies this package in the module layout.
const Seat = "notify"
